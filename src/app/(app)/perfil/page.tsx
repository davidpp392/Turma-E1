import ProfilePageClient from '@/components/perfil/ProfilePageClient';

export const metadata = { title: 'Perfil — Site da Turma' };

export default function PerfilPage() {
  return <ProfilePageClient />;
}
